meta {
  name: Create Category
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}categories
  body: multipartForm
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{TOKEN}}
}

body:multipart-form {
  name: Beras
}
